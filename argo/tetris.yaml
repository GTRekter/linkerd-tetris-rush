# Tetris Rush application — one Application per cluster with cluster-specific
# values matching the Helm overrides from k3d.sh.
#
# Update repoURL and targetRevision to match your Git repository.
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tetris-ap-east
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: tetris
  source:
    # Update this to your actual repo URL
    repoURL: "https://github.com/YOUR_ORG/linkerd-tetris-rush.git"
    targetRevision: main
    path: helm/tetris
    helm:
      valuesObject:
        dashboard:
          enabled: true
          image:
            repository: dashboard
            tag: local
        redis:
          deploy: true
          url: "redis://redis.tetris.svc.cluster.local:6379"
        dashboardApi:
          image:
            repository: dashboard-api
            tag: local
        game:
          image:
            repository: game
            tag: local
        gameApi:
          image:
            repository: game-api
            tag: local
        cluster:
          name: tetris-ap-east
          region: tetris-ap-east
          color: "#3b82f6"
        externalUrl: "http://tetris-ap-east.localhost:8080"
  destination:
    server: "https://tetris-ap-east:6443"
    namespace: tetris
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tetris-ap-central
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: tetris
  source:
    # Update this to your actual repo URL
    repoURL: "https://github.com/YOUR_ORG/linkerd-tetris-rush.git"
    targetRevision: main
    path: helm/tetris
    helm:
      valuesObject:
        dashboard:
          enabled: false
          image:
            repository: dashboard
            tag: local
        redis:
          deploy: false
          url: "redis://redis-tetris-ap-east.tetris.svc.cluster.local:6379"
        dashboardApi:
          image:
            repository: dashboard-api
            tag: local
        game:
          image:
            repository: game
            tag: local
        gameApi:
          image:
            repository: game-api
            tag: local
        cluster:
          name: tetris-ap-central
          region: tetris-ap-central
          color: "#8b5cf6"
        externalUrl: "http://tetris-ap-central.localhost:8081"
  destination:
    server: "https://tetris-ap-central:6443"
    namespace: tetris
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tetris-ap-south
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: tetris
  source:
    # Update this to your actual repo URL
    repoURL: "https://github.com/YOUR_ORG/linkerd-tetris-rush.git"
    targetRevision: main
    path: helm/tetris
    helm:
      valuesObject:
        dashboard:
          enabled: false
          image:
            repository: dashboard
            tag: local
        redis:
          deploy: false
          url: "redis://redis-tetris-ap-east.tetris.svc.cluster.local:6379"
        dashboardApi:
          image:
            repository: dashboard-api
            tag: local
        game:
          image:
            repository: game
            tag: local
        gameApi:
          image:
            repository: game-api
            tag: local
        cluster:
          name: tetris-ap-south
          region: tetris-ap-south
          color: "#06b6d4"
        externalUrl: "http://tetris-ap-south.localhost:8082"
  destination:
    server: "https://tetris-ap-south:6443"
    namespace: tetris
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
